# DRL Model Development Environment Configuration
# Copy this file to .env and update the paths

# LibTorch installation path
# Download from: https://pytorch.org/
# Select: Stable > Windows > LibTorch > C++/Java > CPU
LIBTORCH_ROOT=C:/path/to/libtorch

# vcpkg installation path
# Clone from: https://github.com/microsoft/vcpkg
# Run: bootstrap-vcpkg.bat
VCPKG_ROOT=C:/path/to/vcpkg

# MetaTrader 5 installation path
# Default: C:/Program Files/MetaTrader 5
MT5_PATH=C:/Program Files/MetaTrader 5

# Trading bot source path
# This is the root directory of the trading bot project
BOT_PATH=C:/path/to/bot

# Build configuration
BUILD_TYPE=Release          # Release or Debug
SKIP_TESTS=false           # Set to true to skip tests during build

# Python configuration
PYTHON_VERSION=3.9         # Python version to use
VENV_PATH=.venv           # Virtual environment path
USE_CUDA=false            # Set to true for GPU support (requires CUDA toolkit)

# Development environment
# These settings are used by VSCode and other IDEs
DEV_TOOLS_PATH=C:/path/to/dev/tools     # Path to development tools
MSVC_PATH=C:/path/to/visual/studio      # Visual Studio installation path
WINDOWS_SDK_VERSION=10.0.19041.0        # Windows SDK version to use

# Optional: MT5 deployment settings
MT5_USER_DATA_PATH=%APPDATA%/MetaQuotes/Terminal    # MT5 user data path
EA_NAME=DRLTrader                                   # Name of the Expert Advisor
SYMBOL=XAUUSD                                       # Trading symbol to use
TIMEFRAME=M15                                       # Trading timeframe

# Logging settings
LOG_LEVEL=INFO            # DEBUG, INFO, WARNING, ERROR
LOG_PATH=logs            # Path to log files
ENABLE_DEBUG_LOG=false   # Enable detailed debug logging

# Memory settings
MAX_LSTM_BATCH=1         # Maximum LSTM batch size
OPTIMIZE_MEMORY=true     # Enable memory usage optimization
GC_THRESHOLD=100MB       # Garbage collection threshold

# Feature processing
FEATURE_NORMALIZATION=true    # Enable feature normalization
CLIP_VALUES=true             # Enable value clipping
USE_POSITION_FEATURES=true   # Include position-related features

# Testing settings
TEST_DATA_PATH=test/data     # Path to test data files
RECORD_TEST_OUTPUT=true      # Save test outputs for comparison
COMPARE_PRECISION=1e-6       # Floating-point comparison precision

# Documentation
# Environment variables can be set in several ways:
# 1. System environment variables (permanent)
# 2. User environment variables (permanent)
# 3. Session environment variables (temporary)
# 4. .env file (development only)
#
# To set up the environment:
# 1. Copy this file to .env
# 2. Update the paths to match your system
# 3. Run setup_env.ps1 or setup_env.bat
#
# Example:
# ./scripts/setup_env.ps1 -LibTorchPath $env:LIBTORCH_ROOT -VcpkgPath $env:VCPKG_ROOT -MT5Path $env:MT5_PATH -BotPath $env:BOT_PATH
#
# For persistent environment variables, add -Persistent switch:
# ./scripts/setup_env.ps1 ... -Persistent
#
# Note: Some paths may contain spaces. Always use quotes when passing paths:
# build.bat "C:/Program Files/MetaTrader 5" ...
